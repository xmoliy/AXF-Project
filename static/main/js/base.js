// $(document).ready(function(){
//     document.documentElement.style.fontSize = innerWidth / 10 + "px";
// })
$(function () {
    //设置根元素的字体大小（基准单位），可以使用em（相对父节点）, rem（相对于根节点）获取相对值
    document.documentElement.style.fontSize = innerWidth / 10 +"px"
})